# v3.9.3 학습 결과 분석 (100K steps)

**날짜**: 2025-10-27  
**버전**: v3.9.3  
**학습 시간**: 4.4분  
**이전 버전**: v3.9.2 (grip_rate=0%, reach_rate=34%)

---

## 📊 최종 결과

| 지표 | v3.9.1 | v3.9.2 | v3.9.3 | 변화 (v3.9.2→v3.9.3) |
|------|--------|--------|--------|---------------------|
| **reach_rate** | 30% | 34% | **26%** | **-8% ❌** |
| **grip_rate** | 0% | 0% | **0%** | **0% ❌** |
| lift_rate | 0% | 0% | 0% | 0% |
| success | 0% | 0% | 0% | 0% |
| **평균 보상** | 2,710 | 2,680 | **2,860** | **+180 ✅** |
| **최소 거리** | 6.4cm | 4.8cm | **8.3cm** | **+3.5cm ❌** |

### 🚨 결론
- ❌ **CRITICAL: grip_rate 여전히 0%** (3번째 실패)
- ❌ **회귀**: reach_rate 34% → 26% (악화)
- ❌ **거리 악화**: 4.8cm → 8.3cm (후퇴!)
- ✅ 평균 보상 증가 (보상 함수 변경 효과)

---

## 🔍 거리 분석

### 최소 거리 Top 10 (후반 600 steps)
```
Step 99600-99900: dist=0.084-0.112m (8.4-11.2cm)
Step 100001: dist=0.083m (8.3cm) ← 🔥 최선!
Step 100101: dist=0.085m (8.5cm)
Step 100201-100301: dist=0.102-0.109m (10-11cm)
```

**핵심 발견**:
- **최소 거리**: 8.3cm (v3.9.2: 4.8cm → 73% 악화!)
- **Attach 조건**: dist < 5cm (완화됨)
- **문제**: 5cm 진입조차 실패

### 거리 분포
```
< 5cm:   0회 ❌
5-10cm:  약 10-15회 (8-9cm)
10-20cm: 약 30-40회
> 20cm:  대부분
```

**v3.9.2 대비**:
- 4.8cm 접근 → 8.3cm (3.5cm 후퇴)
- 5cm 이하 진입 0회 (v3.9.2: 1회)

---

## 🎯 그리퍼 분석

### Step 99600-100101 (최선 구간)
```
Step 99601: dist=0.085m, width=0.0412 (4.1cm) ← 이상적!
Step 99701: dist=0.088m, width=0.0459 (4.6cm) ← 이상적!
Step 99801: dist=0.085m, width=0.0577 (5.8cm) ← 약간 벌어짐
Step 99901: dist=0.097m, width=0.0414 (4.1cm) ← 이상적!
Step 100001: dist=0.083m, width=0.0500 (5.0cm) ← 거의 이상적!
```

**패턴 분석**:
- ✅ 그리퍼 폭: 4-5.8cm (이상적 범위!)
- ❌ 거리: 8-10cm (너무 멀음)
- ⚠️ **역설**: 그리퍼는 개선, 거리는 악화

### Attach 조건 체크 (v3.9.3)
```python
dist < 0.05 and              # ❌ 실제: 0.083m (5cm 초과)
2.5 < width < 5.5 and        # ✅ 4-5cm (조건 만족!)
z_align < 0.015              # ✅ 대부분 OK

# 결과: 거리 실패 (5cm 진입 못함)
```

---

## 💡 근본 원인 3가지

### 1. **보상 함수 부작용** (가장 심각) ⚠️
```python
# v3.9.3 변경
if dist_to_cube < 0.10:  # 근거리
    if 0.03 < gripper_width < 0.06:
        reward += 5.0  # 이상적 범위
    elif gripper_width > 0.01:
        reward += 1.0  # 약화 (기존 3.0 → 1.0)

# 부작용:
# - 근거리 보상 약화 (3.0 → 1.0)
# - 접근 동기 감소
# - 결과: 8-10cm에서 안주
```

### 2. **연동 보상 조건 너무 엄격**
```python
# v3.9.3 추가
if dist < 0.05 and 0.03 < width < 0.06:
    reward += 30.0  # 강력한 보상!

# 문제:
# - 5cm 진입 못함 (8.3cm)
# - 조건 만족 불가 → 보상 받을 수 없음
# - 학습 신호 없음
```

### 3. **조건 완화 효과 없음**
```python
# v3.9.3: width 2.5-5.5cm (완화)
# 실제 그리퍼: 4-5.8cm ✅

# 하지만:
# - 거리 8.3cm (목표: < 5cm)
# - 폭 조건 완화해도 거리 실패
# - Attach 여전히 불가능
```

---

## 📊 버전 비교 (핵심 지표)

| 버전 | dist_tol | width_range | grip_rate | reach_rate | 최소 거리 |
|------|----------|-------------|-----------|------------|----------|
| v3.9.1 | 3cm | 3.4-4.6cm | 0% | 30% | 6.4cm |
| v3.9.2 | 5cm | 3.2-4.8cm | 0% | 34% | **4.8cm** ✅ |
| v3.9.3 | 5cm | 2.5-5.5cm | 0% | 26% | **8.3cm** ❌ |

**결론**: v3.9.2가 가장 좋았음 (4.8cm 달성)

---

## 🔥 문제 진단

### **왜 v3.9.3이 v3.9.2보다 나빠졌나?**

#### 1. 보상 함수 변경 역효과
```python
# v3.9.2 (기존)
if gripper_width > 0.01:
    reward += 3.0  # 무조건 +3.0

# v3.9.3 (변경)
if dist < 0.10:
    if gripper_width > 0.01:
        reward += 1.0  # 약화! (3.0 → 1.0)

# 결과:
# - 근거리 접근 동기 감소
# - 10cm에서 안주 (1.0 보상으로 충분)
# - 5cm 진입 의지 감소
```

#### 2. 연동 보상 너무 멀리 떨어짐
```python
# v3.9.3 추가
if dist < 0.05 and width 3-6cm:
    reward += 30.0  # 강력하지만 도달 불가

# 문제:
# - 현재 위치: 8-10cm
# - 목표: 5cm
# - Gap: 3-5cm (너무 멀어서 신호 없음)
```

#### 3. v3.9.2 성공 요인 파괴
```python
# v3.9.2 성공 포인트:
# - 3cm 이하 보상 +20 (강력)
# - 거리 보상 일관성 유지
# - 결과: 4.8cm 달성 (거의 성공)

# v3.9.3 변경:
# - 근거리 보상 약화 (3.0 → 1.0)
# - 일관성 파괴
# - 결과: 8.3cm 후퇴
```

---

## 💡 해결 방안 (v3.9.4)

### **전략: v3.9.2로 롤백 + 미세 조정**

#### 1. v3.9.2 기반 유지 ⭐⭐⭐
```python
# 보상 함수: v3.9.2 복원
if gripper_width > 0.01:
    reward += 3.0  # 원래대로 (일관성)

# 근접 보상: v3.9.2 유지
if dist < 0.03:
    reward += 20.0

# Attach 조건: v3.9.3 유지
width_margin=0.015  # 2.5-5.5cm (완화 유지)
```

#### 2. 단계별 근접 보상 추가 ⭐⭐
```python
# 점진적 보상 (연동 대신)
if dist < 0.07:
    reward += 10.0  # 7cm 이하
if dist < 0.05:
    reward += 20.0  # 5cm 이하
if dist < 0.03:
    reward += 30.0  # 3cm 이하 (기존 +20 강화)
```

#### 3. 연동 보상 조건 완화 ⭐
```python
# 현재 (v3.9.3): 너무 엄격
if dist < 0.05 and width 3-6cm:
    reward += 30.0

# 제안 (v3.9.4): 단계별 완화
if dist < 0.10 and width 3-6cm:  # 10cm로 완화
    reward += 10.0  # 초기 신호
if dist < 0.05 and width 3-6cm:
    reward += 40.0  # 최종 목표 (30→40 강화)
```

---

## 📋 v3.9.4 구현 계획

### Step 1: 보상 함수 롤백 + 강화 (10분)
```python
# 1. v3.9.2 기반 보상 복원
if gripper_width > 0.01:
    reward += 3.0  # 원래대로

# 2. 단계별 근접 보상
if dist < 0.07:
    reward += 10.0
if dist < 0.05:
    reward += 20.0
if dist < 0.03:
    reward += 30.0  # 강화 (기존 +20)

# 3. 연동 보상 완화
if dist < 0.10 and width 3-6cm:
    reward += 10.0
if dist < 0.05 and width 3-6cm:
    reward += 40.0
```

### Step 2: Attach 조건 유지
```python
# v3.9.3 조건 유지 (최선)
dist_tol=0.05,        # 5cm
width_margin=0.015    # ±15mm (2.5-5.5cm)
```

### Step 3: v3.9.4 학습 (100K, 4분)
```bash
~/isaacsim/python.sh scripts/rl/train_dense_reward.py --timesteps 100000
```

---

## 🎯 예상 효과

### 시나리오 1: 기본 성공
- **최소 거리**: 8.3cm → 4-5cm
- **grip_rate**: 0% → 10-20%
- **근거**: v3.9.2 수준 회복 + 폭 조건 완화

### 시나리오 2: 돌파 성공
- **최소 거리**: 3-4cm
- **grip_rate**: 0% → 30-40%
- **근거**: 단계별 보상 + Attach 조건 완화

### 시나리오 3: 최악
- **grip_rate**: 0% → < 5%
- **대응**: 
  * dist < 0.08 보상 추가
  * 보상 배율 증가 (×1.5)

---

## 📚 핵심 교훈

### 🔴 **v3.9.3 실패 원인**
1. **보상 일관성 파괴**: 근거리 보상 약화 (3.0 → 1.0)
2. **너무 큰 변화**: 2개 변수 동시 수정 (조건 + 보상)
3. **검증 없이 진행**: v3.9.2 성공 요인 분석 부족

### 🟢 **v3.9.2 성공 요인**
1. ✅ **거리 중심 전략**: 4.8cm 달성
2. ✅ **보상 일관성**: 그리퍼 보상 유지
3. ✅ **점진적 완화**: dist_tol만 변경

### 📝 **교훈**
> **"한 번에 하나씩, 작은 변화로"**
> - v3.9.2: 조건만 완화 (성공 - 4.8cm)
> - v3.9.3: 조건 + 보상 (실패 - 8.3cm)
> - v3.9.4: v3.9.2 기반 + 미세 조정

---

## 🚀 다음 단계

1. ✅ **v3.9.4 구현** (10분)
   - v3.9.2 보상 복원
   - 단계별 근접 보상
   - Attach 조건 유지 (v3.9.3)
   
2. ⏳ **v3.9.4 학습** (100K, 4분)
   - 목표: 최소 거리 < 5cm
   - 목표: grip_rate > 10%
   
3. ⏳ **결과 검증**
   - v3.9.2 수준 회복 확인
   - ATTACH 이벤트 발생 확인

---

## 💬 최종 권고

**v3.9.4 전략**:
- ✅ v3.9.2 보상 구조 복원 (입증된 방법)
- ✅ v3.9.3 Attach 조건 유지 (2.5-5.5cm)
- ✅ 단계별 보상 추가 (점진적 유도)
- ✅ 한 번에 하나씩 검증

**예상 결과**:
- 최소 거리: 4-5cm 회복 (v3.9.2 수준)
- grip_rate: 10-30% (Attach 성공!)
