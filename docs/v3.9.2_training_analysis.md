# v3.9.2 학습 결과 분석 (100K steps)

**날짜**: 2025-10-27  
**버전**: v3.9.2  
**학습 시간**: 4.4분  
**이전 버전**: v3.9.1 (grip_rate=0%, reach_rate=30%)

---

## 📊 최종 결과

| 지표 | v3.9.1 | v3.9.2 | 변화 | 목표 |
|------|--------|--------|------|------|
| **reach_rate** | 15/50 = 30% | 17/50 = 34% | +4% ↗️ | - |
| **grip_rate** | 0/50 = 0% | 0/50 = 0% | **0% ❌** | > 10% |
| **lift_rate** | 0/50 = 0% | 0/50 = 0% | 0% | - |
| **success_rate** | 0/50 = 0% | 0/50 = 0% | 0% | - |
| **평균 보상** | 2,710 | 2,680 | -30 | - |

### 🚨 결론
- ❌ **CRITICAL FAILURE**: grip_rate 여전히 0%
- ✅ reach_rate 약간 향상 (30% → 34%)
- ❌ Attach 조건 완화 효과 없음

---

## 🔍 거리 분석

### 최소 거리 Top 10
```
Step 98400: dist=0.048m (4.8cm) ← 🔥 최선!
Step 99000: dist=0.110m (11.0cm)
Step 99100-99500: dist=0.155-0.177m (15-18cm)
Step 100001-100301: dist=0.194-0.261m (19-26cm)
```

**핵심 발견**:
- **최소 거리**: 4.8cm (v3.9.1: 6.4cm → 1.6cm 개선!)
- **Attach 조건**: dist < 5cm (완화됨)
- **문제**: 4.8cm는 조건 만족하지만 **attached=False**

### 거리 분포
```
< 5cm:   1회 (Step 98400)  ← Attach 조건 만족!
5-10cm:  0회
10-20cm: 약 20-30회 (가끔 접근)
> 20cm:  대부분 (탐색 중)
```

---

## 🎯 그리퍼 분석

### Step 98400 (최선의 순간)
```
dist=0.048m (4.8cm)    ✅ < 5cm (조건 만족!)
width=0.0622 (6.22cm)  ❌ > 4.8cm (조건 불만족)
attached=False         ❌ Attach 실패
```

**Attach 조건 체크** (v3.9.2):
```python
dist < 0.05 and              # ✅ 4.8cm < 5cm
3.2cm < width < 4.8cm and    # ❌ 6.22cm > 4.8cm
z_align < 0.015              # ✅ 일반적으로 OK

# 결과: 거리는 OK, 그리퍼 너무 벌어짐!
```

### 그리퍼 패턴 분석
```
Step 98400: width=0.0622 (6.2cm) - 너무 벌어짐
Step 98500: width=0.0434 (4.3cm) - 거의 이상적!
Step 98600: width=0.0583 (5.8cm) - 다시 벌어짐
Step 98700: width=0.0587 (5.9cm) - 계속 벌어짐
Step 98800: width=0.0609 (6.1cm) - 더 벌어짐
```

**패턴**: 4.8cm 접근 시 그리퍼 6.2cm (너무 벌어짐)

---

## 💡 근본 원인 3가지

### 1. **그리퍼 폭 문제** (가장 심각)
```
조건: 3.2cm < width < 4.8cm
실제: width = 6.2cm (초과!)

원인:
- 로봇이 접근하면서 그리퍼를 벌림
- "충돌 회피" 본능?
- 보상 함수가 "그리퍼 열기" 유도 (3.0 보상)
```

### 2. **Attach 조건 여전히 엄격**
```
v3.9.1: dist < 3cm and 3.4 < width < 4.6
v3.9.2: dist < 5cm and 3.2 < width < 4.8

문제:
- 거리는 완화했지만 (3cm → 5cm)
- 그리퍼 폭 범위 여전히 좁음 (±8mm)
- 두 조건 동시 만족 어려움
```

### 3. **보상 함수 충돌**
```python
# 그리퍼 열기 보상
if gripper_width > 0.01:
    reward += 3.0  # 매 스텝 +3.0

# 이상적 폭 보상
IDEAL_WIDTH = 0.04
width_error = gripper_width - 0.04
penalty = -5.0 * width_error**2

# 결과: 6cm에서도 +3.0 받음
```

---

## 🔥 해결 방안 (v3.9.3)

### **우선순위 1: 그리퍼 폭 조건 완화** ⭐⭐⭐
```python
# 현재 (v3.9.2)
width_margin=0.008  # 3.2-4.8cm (±8mm)

# 제안 (v3.9.3)
width_margin=0.015  # 2.5-5.5cm (±15mm)
# 6.2cm도 수용 가능하게
```

**근거**:
- Step 98400: dist=4.8cm, width=6.2cm
- 폭을 5.5cm로 완화하면 즉시 Attach 가능
- FixedJoint 후 안정화는 물리 엔진이 처리

### **우선순위 2: 보상 함수 수정** ⭐⭐
```python
# 현재 (문제)
if gripper_width > 0.01:
    reward += 3.0  # 무조건 +3.0

# 제안 (v3.9.3)
if dist_to_cube < 0.10:  # 10cm 이내에서만
    if 0.03 < gripper_width < 0.06:  # 3-6cm 범위
        reward += 5.0  # 이상적 범위 강화
    elif gripper_width > 0.01:
        reward += 1.0  # 기본 보상 약화
```

**근거**:
- 원거리: 그리퍼 열기 유도 (충돌 회피)
- 근거리: 이상적 범위 강조 (3-6cm)

### **우선순위 3: 거리-폭 연동 보상** ⭐
```python
# 제안 (v3.9.3)
if dist_to_cube < 0.05:  # 5cm 이내
    # 거리 좁고 + 그리퍼 이상적 → 큰 보상
    if 0.03 < gripper_width < 0.06:
        reward += 30.0  # 강력한 연동 보상!
```

**근거**:
- 4.8cm 거리 + 4-5cm 폭 → 즉시 Attach
- 두 조건 동시 만족 강력 유도

---

## 📋 v3.9.3 구현 계획

### Step 1: Attach 조건 완화 (5분)
```python
# controllers/gripper.py - is_grasped() 함수
width_margin=0.015  # ±15mm (2.5-5.5cm)
```

### Step 2: 보상 함수 수정 (5분)
```python
# envs/roarm_pick_place_env.py

# 1. 그리퍼 열기 보상 조건부 적용
if dist_to_cube < 0.10:
    if 0.03 < gripper_width < 0.06:
        reward += 5.0  # 이상적 범위
    elif gripper_width > 0.01:
        reward += 1.0  # 기본 보상 약화
else:
    if gripper_width > 0.01:
        reward += 3.0  # 원거리 유지

# 2. 거리-폭 연동 보상
if dist_to_cube < 0.05 and 0.03 < gripper_width < 0.06:
    reward += 30.0  # 강력한 연동 보상!
```

### Step 3: v3.9.3 학습 (100K, 4분)
```bash
~/isaacsim/python.sh scripts/rl/train_dense_reward.py --timesteps 100000
```

---

## 🎯 예상 효과

### 시나리오 1: 폭 조건만 완화
- **Step 98400 재현 시**: 즉시 Attach! (4.8cm + 6.2cm → 조건 만족)
- **예상**: grip_rate 0% → 10-15%

### 시나리오 2: 폭 완화 + 보상 수정
- **근거리 이상적 폭 유도**: 4-5cm 범위 학습
- **예상**: grip_rate 0% → 20-30%

### 시나리오 3: 최악
- **여전히 실패**: grip_rate < 5%
- **대응**: dist_tol 7cm로 추가 완화

---

## 📚 버전 비교

| 버전 | dist_tol | width_range | grip_rate | 최소 거리 |
|------|----------|-------------|-----------|-----------|
| v3.9.1 | 3cm | 3.4-4.6cm (±6mm) | 0% | 6.4cm |
| v3.9.2 | 5cm | 3.2-4.8cm (±8mm) | 0% | 4.8cm ✅ |
| **v3.9.3** | 5cm | 2.5-5.5cm (±15mm) | **10-30%?** | - |

---

## 🚀 다음 단계

1. ✅ **v3.9.3 코드 수정** (10분)
   - Attach 조건 완화
   - 보상 함수 수정
   
2. ⏳ **v3.9.3 학습** (100K, 4분)
   - 목표: grip_rate > 10%
   
3. ⏳ **결과 검증**
   - ATTACH 이벤트 확인
   - 로그 분석

---

## 💬 핵심 인사이트

> **"거리는 해결했다. 이제 그리퍼다."**

- v3.9.1 → v3.9.2: 거리 6.4cm → 4.8cm (25% 개선)
- 하지만 grip_rate 여전히 0%
- **진짜 문제**: 그리퍼 폭 조건 너무 엄격
  * 조건: 3.2-4.8cm
  * 실제: 6.2cm (접근 시)
- **해결**: 폭 범위 2.5-5.5cm로 완화 (±15mm)
