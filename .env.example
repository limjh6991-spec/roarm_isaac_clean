# Isaac Sim 환경 변수 (커밋 가능)
# 각 사용자의 환경에 맞게 수정 필요

# =============================================================================
# Isaac Sim 경로
# =============================================================================
ISAAC_SIM_ROOT=/home/roarm_m3/isaacsim
ISAAC_PYTHON=${ISAAC_SIM_ROOT}/python.sh

# =============================================================================
# 프로젝트 경로
# =============================================================================
PROJECT_ROOT=/home/roarm_m3/roarm_isaac_clean
ASSETS_ROOT=${PROJECT_ROOT}/assets
DATA_ROOT=${PROJECT_ROOT}/data
LOGS_ROOT=${PROJECT_ROOT}/logs
MODELS_ROOT=${PROJECT_ROOT}/models

# =============================================================================
# GPU 설정
# =============================================================================
# Vulkan device (0 = first GPU)
VULKAN_DEVICE=0

# Use EGL for headless rendering
KIT_USE_EGL=1

# CUDA device (멀티 GPU 시스템의 경우)
# CUDA_VISIBLE_DEVICES=0

# =============================================================================
# Python 설정
# =============================================================================
# Unbuffered output (로그 즉시 출력)
PYTHONUNBUFFERED=1

# Python path (환경 import를 위해)
# PYTHONPATH=${PROJECT_ROOT}  # Makefile에서 설정됨

# =============================================================================
# Isaac Sim 실행 설정
# =============================================================================
# Headless mode (GUI 없이 실행)
ISAAC_HEADLESS=true

# 렌더링 해상도
ISAAC_WIDTH=640
ISAAC_HEIGHT=480

# =============================================================================
# 강화학습 설정
# =============================================================================
# RL 가상환경 경로
RL_VENV=${PROJECT_ROOT}/envs/rl-venv

# 기본 학습 스텝 수
TRAIN_TIMESTEPS=50000

# 병렬 환경 개수
NUM_ENVS=16

# =============================================================================
# 로깅 설정
# =============================================================================
# Weights & Biases (기본 비활성화)
WANDB_DISABLED=true
# WANDB_PROJECT=roarm_m3_rl
# WANDB_ENTITY=your_username

# TensorBoard 로그 디렉토리
TENSORBOARD_LOG_DIR=${PROJECT_ROOT}/scripts/rl/tensorboard_logs

# =============================================================================
# 실험 설정
# =============================================================================
# Experiment name (타임스탬프 자동 추가)
EXPERIMENT_NAME=roarm_dense_reward

# Checkpoint 저장 간격 (steps)
CHECKPOINT_FREQ=10000

# =============================================================================
# 디버그 설정
# =============================================================================
# 디버그 모드 (상세 로그 출력)
DEBUG=false

# Preflight 체크 건너뛰기 (비추천)
SKIP_PREFLIGHT=false
